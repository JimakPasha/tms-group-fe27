242 строка - так как постоянно нужно будет обновлять наш localStorage - напишем функцию, которая будет переиспользоваться везде

120 строка - создаём массив, в котором будут объекты todoItem со всеми полями которые требуются в тз(техническое задание)

122 строка - проверяем, есть ли что-то в localStorage, если есть, то в наш массив закидываем данные оттуда, не забываем парсить

124 строка - бежим циклом по каждому объекту и создаём элемент на основе объект

125 - это функция которая описана на 187 строке

187 - универсальная функция, которая будет создавать нам iteProducts, со всеми внутренностями (изучите её как следует)

216 - проверка на то, что элемент чекнут, если нет, то даём ему opacity

220 - обработчик который повесли на наш temProducts 

221 - проверяем на чекед уже при клике на элемент

226 - также при клике меняем наш todoArr. Смотрим если элемент todoArr id равен объекту id (item.id === obj.id), тогда в нашем item id меняем поле isChecked.

232 - сетаем в localStorage массив todoArr

235 - удаляем элемент по нажатию кнопки 'Удалить'.
Удаляем из нашего массива элемент если айдишник совпадает, фильтруя наш массив. Сетаем наш массив в localStorage

130 - при нажатии на добавление itemProducts создаём объект, и заполняем поля согласно тз(техническое задание)

137 - пушим наш новый объект в массив. Сетаем обновлённый массив в localStorage

144 - по удалению обнуляем наш storage и массив, а также внутренность listProducts

150 - Тут по кнопке 'Удаление последнего элемента' удаляем последний элемент и обновляем localStorage